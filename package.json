{
  "name": "opentrpg",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "npm run dev -w opentrpg-client",
    "server": "npm run start -w opentrpg-server",
    "build": "npm run build -w opentrpg-client"
  }
}
